# Releases Directory

This directory tracks small release metadata files that are useful to keep in git,
such as checksums and release-specific notes.

Do not commit large binary release archives into the repository history unless you
have a specific reason to do so. Publish those files as GitHub release assets
instead.
